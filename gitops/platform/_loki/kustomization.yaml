apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# PLACEHOLDER: excluded from the ApplicationSet. Rename this directory to `loki`
# to activate it.
namespace: logging
helmCharts:
  - name: loki
    repo: https://grafana.github.io/helm-charts
    version: 7.3.0
    releaseName: loki
    namespace: logging
    valuesFile: values.yaml
